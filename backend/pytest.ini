[pytest]
asyncio_mode = auto
testpaths = tests
# Run tests in parallel across CPU cores. Each xdist worker gets its own database
# (see tests/conftest.py) so tests stay isolated. Override with `-n0` to run serially
# when debugging (e.g. for pdb or `-s`).
addopts = -n auto
