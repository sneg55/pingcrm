# Pinned to resolve starlette 1.x. fastapi 0.115 capped starlette at <0.39,
# which held it on 0.38.6 — vulnerable to CVE-2024-47874 (multipart DoS),
# CVE-2025-54121, CVE-2026-48710 (Host header validation) and CVE-2026-48817.
# Do not pin starlette directly; it collides with fastapi's own cap.
#
# Held at 0.136.x deliberately: 0.137+ makes include_router lazy (routes become
# fastapi.routing._IncludedRouter wrappers), which breaks every site that walks
# app.routes directly — tests/test_route_inventory.py and
# scripts/check_response_models.py. Moving past 0.136 requires teaching those to
# flatten the wrappers first. Routing itself is unaffected.
fastapi==0.136.3
uvicorn[standard]==0.32.1
sqlalchemy==2.0.35
alembic==1.13.0
asyncpg==0.30.0
pydantic==2.11.10
pydantic-settings>=2.5.0
PyJWT[crypto]==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.31
httpx>=0.27.0,<1
celery[redis]==5.4.0
redis==5.1.0
fakeredis==2.34.1
python-dotenv==1.2.2
google-api-python-client==2.150.0
google-auth-oauthlib==1.2.1
anthropic==0.40.0
telethon==1.37.0
cryptography==50.0.0
email-validator>=2.0.0
python-json-logger>=3.0.0
mcp>=1.28.1,<1.29.0

# Transitive pins for security only — these are pulled in by alembic (Mako),
# requests/google-auth (urllib3, pyasn1). Floor them so a fresh build cannot
# resolve back to the known-vulnerable versions.
urllib3>=2.7.0
Mako>=1.3.12
pyasn1>=0.6.3
